require('dotenv').config();
const server = require('./server');
// const salesforce = require('./services/salesforce/connect');
// const query = require('./services/salesforce/query');

// salesforce.login();

// query.getUsers()
//   .then(name => console.log('all records', name))
// query.getUsers()
//   .then(name => console.log('all records', name))